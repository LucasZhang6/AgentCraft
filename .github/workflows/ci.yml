name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

jobs:
  go-matrix:
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, macos-latest, windows-latest]
    runs-on: ${{ matrix.os }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.27.0'
          cache-dependency-path: examples/your-agent/go.sum
      - name: Test Go packages
        shell: bash
        run: |
          set +e
          go test -v ./examples/your-agent/... 2>&1 | tee go-test.log
          status=${PIPESTATUS[0]}
          if [[ $status -ne 0 ]]; then
            message=$(tail -n 100 go-test.log)
            message=${message//'%'/'%25'}
            message=${message//$'\r'/'%0D'}
            message=${message//$'\n'/'%0A'}
            echo "::error title=Go tests failed on ${RUNNER_OS}::${message}"
          fi
          exit "$status"
      - shell: bash
        run: |
          mkdir -p dist/bin
          suffix=""; [[ "${{ runner.os }}" == "Windows" ]] && suffix=".exe"
          go build -o "dist/bin/your-agent${suffix}" ./examples/your-agent/cmd/your-agent
          go build -o "dist/bin/your-agent-server${suffix}" ./examples/your-agent/cmd/your-agent-server
          go build -o "dist/bin/feishu-adapter${suffix}" ./examples/your-agent/cmd/feishu-adapter

  open-source-and-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version: '1.27.0'
          cache-dependency-path: examples/your-agent/go.sum
      - uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24
          cache: npm
      - run: npm ci
      - run: npm --prefix ai-agent-roadmap-site ci
      - run: make open-source-check
      - run: npx playwright install --with-deps chromium
      - run: make browser-regression
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        if: always()
        with:
          name: browser-regression
          path: artifacts/browser
